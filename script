-- SWILL | MM2 Coin Collector (Universal Search)
-- Ищет монеты по всему workspace

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

-- ===== НАСТРОЙКИ =====
local TELEPORT_OFFSET = 2.5
local COLLECT_INTERVAL = 0.15

-- ===== ПЕРЕМЕННЫЕ =====
local collected = 0
local running = true
local lastCollect = 0

-- ===== ПОИСК МОНЕТ ПО ВСЕМУ WORKSPACE =====
local function getAllCoins()
    local coins = {}
    
    for _, obj in pairs(workspace:GetDescendants()) do
        -- Проверяем по имени (регистр не важен)
        local name = obj.Name:lower()
        if name == "coin" or name:find("coin") then
            -- Если это модель, ищем в ней Handle или любую часть
            if obj:IsA("Model") then
                local handle = obj:FindFirstChild("Handle")
                if handle and handle:IsA("BasePart") then
                    table.insert(coins, handle)
                else
                    -- Если Handle нет, берём любую BasePart
                    for _, part in pairs(obj:GetChildren()) do
                        if part:IsA("BasePart") then
                            table.insert(coins, part)
                            break
                        end
                    end
                end
            elseif obj:IsA("BasePart") then
                -- Если это сама часть
                table.insert(coins, obj)
            end
        end
    end
    
    return coins
end

-- ===== ПОИСК БЛИЖАЙШЕЙ МОНЕТЫ =====
local function findNearestCoin()
    local character = LocalPlayer.Character
    if not character then return nil end
    
    local root = character:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    
    local rootPos = root.Position
    local coins = getAllCoins()
    
    if #coins == 0 then return nil end
    
    local nearest = nil
    local nearestDist = math.huge
    
    for _, coin in pairs(coins) do
        if coin and coin:IsA("BasePart") then
            local dist = (coin.Position - rootPos).Magnitude
            if dist < nearestDist then
                nearestDist = dist
                nearest = coin
            end
        end
    end
    
    return nearest, nearestDist
end

-- ===== ТЕЛЕПОРТ =====
local function teleportToCoin(coin)
    local character = LocalPlayer.Character
    if not character then return false end
    
    local root = character:FindFirstChild("HumanoidRootPart")
    if not root then return false end
    
    if tick() - lastCollect < COLLECT_INTERVAL then
        return false
    end
    
    local pos = coin.Position
    root.CFrame = CFrame.new(pos + Vector3.new(0, TELEPORT_OFFSET, 0))
    
    lastCollect = tick()
    collected = collected + 1
    
    return true
end

-- ===== GUI =====
local function createGUI()
    local ScreenGui = Instance.new("ScreenGui")
    local Frame = Instance.new("Frame")
    local TextLabel = Instance.new("TextLabel")
    local CloseButton = Instance.new("TextButton")
    
    ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    ScreenGui.Name = "SWILL_CoinCollector"
    ScreenGui.ResetOnSpawn = false
    
    Frame.Parent = ScreenGui
    Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Frame.BackgroundTransparency = 0.6
    Frame.Size = UDim2.new(0, 200, 0, 50)
    Frame.Position = UDim2.new(0, 10, 0, 10)
    Frame.BorderSizePixel = 0
    
    TextLabel.Parent = Frame
    TextLabel.BackgroundTransparency = 1
    TextLabel.TextColor3 = Color3.fromRGB(255, 215, 0)
    TextLabel.Size = UDim2.new(1, 0, 1, 0)
    TextLabel.Text = "SWILL | 0 монет"
    TextLabel.Font = Enum.Font.GothamBold
    TextLabel.TextSize = 16
    TextLabel.TextScaled = true
    
    CloseButton.Parent = Frame
    CloseButton.BackgroundTransparency = 1
    CloseButton.Size = UDim2.new(0, 25, 0, 25)
    CloseButton.Position = UDim2.new(1, -30, 0, 5)
    CloseButton.Text = "X"
    CloseButton.TextColor3 = Color3.fromRGB(255, 0, 0)
    CloseButton.TextScaled = true
    CloseButton.Font = Enum.Font.GothamBold
    
    CloseButton.MouseButton1Click:Connect(function()
        running = false
        ScreenGui:Destroy()
        print("[SWILL] Сбор остановлен")
    end)
    
    task.spawn(function()
        while running and ScreenGui.Parent do
            task.wait(0.5)
            TextLabel.Text = "SWILL | " .. collected .. " монет"
        end
    end)
    
    return ScreenGui
end

-- ===== ОСНОВНОЙ ЦИКЛ =====
local function main()
    print("[SWILL] Запуск авто-сбора монет (универсальный поиск)")
    
    createGUI()
    
    while running do
        task.wait(0.1)
        
        local character = LocalPlayer.Character
        if not character then
            task.wait(1)
            continue
        end
        
        local root = character:FindFirstChild("HumanoidRootPart")
        if not root then
            task.wait(1)
            continue
        end
        
        local coin = findNearestCoin()
        if coin then
            teleportToCoin(coin)
        else
            -- Если монет нет, просто ждём
            task.wait(0.5)
        end
    end
end

task.spawn(main)

print("[SWILL] Скрипт загружен. Нажми X на GUI для остановки.")
