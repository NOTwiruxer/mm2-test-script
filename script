--[[
    MM2 COIN FARM v3 - С РЕГУЛИРОВКОЙ ВЫСОТЫ
    - Подбирает оптимальную высоту под карту
    - Ползунок для ручной настройки
    - Визуальный индикатор
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer

-- ===== НАСТРОЙКИ =====
local config = {
    enabled = false,
    underMapOffset = Vector3.new(0, -8, 0),  -- СТАРТОВАЯ ВЫСОТА (теперь -8, а не -35)
    collectionDistance = 10,                  -- Радиус сбора
    flySpeed = 50,
    toggleKey = Enum.KeyCode.F1,
    useUnderMap = true,
    heightStep = 1,                          -- Шаг изменения высоты
}

-- ===== ПЕРЕМЕННЫЕ =====
local character = nil
local humanoid = nil
local rootPart = nil
local noclipParts = {}
local isFarming = false
local collectConnection = nil
local coinCount = 0
local currentHeight = -8  -- Текущая высота под картой

-- ===== ПОЛУЧЕНИЕ ПЕРСОНАЖА =====
local function getCharacter()
    character = player.Character
    if character then
        humanoid = character:FindFirstChild("Humanoid")
        rootPart = character:FindFirstChild("HumanoidRootPart")
    end
    return character ~= nil
end

-- ===== НОКЛИП =====
local function enableNoclip()
    if not character then return end
    noclipParts = {}
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
            table.insert(noclipParts, part)
        end
    end
end

local function disableNoclip()
    for _, part in ipairs(noclipParts) do
        if part and part.Parent then
            part.CanCollide = true
        end
    end
    noclipParts = {}
end

-- ===== ПОИСК МОНЕТ =====
local function findCoins()
    local coins = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") then
            local name = obj.Name:lower()
            if name:find("coin") or name:find("money") or name:find("gold") or name:find("cash") then
                table.insert(coins, obj)
            elseif obj:FindFirstChild("ClickDetector") then
                table.insert(coins, obj)
            elseif obj:FindFirstChild("TouchEvent") or obj:FindFirstChild("CollectEvent") then
                table.insert(coins, obj)
            end
        end
    end
    return coins
end

-- ===== ПОИСК БЛИЖАЙШЕЙ МОНЕТЫ =====
local function findClosestCoin()
    if not rootPart then return nil end
    local coins = findCoins()
    if #coins == 0 then return nil end
    
    local closest = nil
    local closestDist = math.huge
    local rootPos = rootPart.Position
    
    for _, coin in ipairs(coins) do
        if coin and coin.Parent then
            local dist = (coin.Position - rootPos).Magnitude
            if dist < closestDist then
                closestDist = dist
                closest = coin
            end
        end
    end
    return closest, closestDist
end

-- ===== ПОДЛЁТ К МОНЕТЕ (С УЧЁТОМ ВЫСОТЫ) =====
local function moveToCoin(coin)
    if not rootPart or not coin or not coin.Parent then return false end
    
    local coinPos = coin.Position
    local targetPos = coinPos
    
    if config.useUnderMap then
        -- Используем текущую высоту
        targetPos = Vector3.new(coinPos.X, coinPos.Y + currentHeight, coinPos.Z)
    end
    
    local currentPos = rootPart.Position
    local distance = (targetPos - currentPos).Magnitude
    
    if distance < config.collectionDistance then
        return true
    end
    
    local direction = (targetPos - currentPos).Unit
    local speed = config.flySpeed * 0.5
    local newPos = currentPos + direction * speed
    
    rootPart.CFrame = CFrame.new(newPos)
    rootPart.Velocity = direction * speed
    
    return false
end

-- ===== СБОР МОНЕТЫ =====
local function collectCoin(coin)
    if not coin or not coin.Parent then return false end
    
    local success = false
    pcall(function()
        local clickDet = coin:FindFirstChild("ClickDetector")
        if clickDet and clickDet:IsA("ClickDetector") then
            clickDet:FireClick(player.Mouse)
            success = true
            return
        end
        
        local remote = coin:FindFirstChild("RemoteEvent") or coin:FindFirstChild("CollectEvent")
        if remote and remote:IsA("RemoteEvent") then
            remote:FireServer(player)
            success = true
            return
        end
        
        local fakePart = Instance.new("Part")
        fakePart.CanCollide = false
        fakePart.Transparency = 1
        fakePart.Size = Vector3.new(0.5, 0.5, 0.5)
        fakePart.Position = coin.Position + Vector3.new(0, 1, 0)
        fakePart.Parent = workspace
        
        if coin.Touched then
            coin.Touched:Fire(fakePart)
            success = true
        end
        
        local collectFunc = coin:FindFirstChild("Collect")
        if collectFunc and typeof(collectFunc) == "function" then
            collectFunc(player)
            success = true
        end
        
        game:GetService("Debris"):AddItem(fakePart, 0.05)
        
        if not success then
            coin.Position = rootPart.Position + Vector3.new(0, -3, 0)
            success = true
        end
    end)
    
    if success then
        coinCount = coinCount + 1
        return true
    end
    return false
end

-- ===== ОСНОВНОЙ ЦИКЛ =====
local function farmingLoop()
    if not rootPart then return end
    
    local closest, dist = findClosestCoin()
    
    if not closest then
        if config.useUnderMap then
            local time = tick()
            local x = math.sin(time * 0.3) * 20
            local z = math.cos(time * 0.4) * 20
            local targetPos = rootPart.Position + Vector3.new(x, 0, z)
            targetPos = Vector3.new(targetPos.X, targetPos.Y + currentHeight, targetPos.Z)
            rootPart.CFrame = CFrame.new(targetPos)
        end
        return
    end
    
    if dist > config.collectionDistance then
        moveToCoin(closest)
        return
    end
    
    if dist <= config.collectionDistance then
        collectCoin(closest)
        task.wait(0.05)
    end
end

-- ===== УПРАВЛЕНИЕ ВЫСОТОЙ =====
local function changeHeight(delta)
    currentHeight = currentHeight + delta
    -- Ограничиваем, чтобы не улететь слишком далеко
    if currentHeight > 0 then currentHeight = 0 end
    if currentHeight < -30 then currentHeight = -30 end
    config.underMapOffset = Vector3.new(0, currentHeight, 0)
    
    -- Обновляем GUI
    local gui = player.PlayerGui:FindFirstChild("MM2FarmGUI")
    if gui then
        local heightLabel = gui.Frame:FindFirstChild("HeightLabel")
        if heightLabel then
            heightLabel.Text = "Высота: " .. currentHeight
        end
    end
    
    print("[MM2 Farm] Высота изменена на: " .. currentHeight)
end

-- ===== ВКЛЮЧЕНИЕ =====
local function startFarming()
    if isFarming then return end
    if not getCharacter() then
        print("[MM2 Farm] ❌ Ошибка: персонаж не найден")
        return
    end
    
    isFarming = true
    config.enabled = true
    enableNoclip()
    
    if collectConnection then collectConnection:Disconnect() end
    collectConnection = RunService.Heartbeat:Connect(function()
        if isFarming and rootPart then
            farmingLoop()
        end
    end)
    
    print("[MM2 Farm] ✅ АВТОФАРМ ВКЛЮЧЁН")
    print("[MM2 Farm] 🔹 Текущая высота: " .. currentHeight)
end

-- ===== ВЫКЛЮЧЕНИЕ =====
local function stopFarming()
    if not isFarming then return end
    isFarming = false
    config.enabled = false
    
    if collectConnection then
        collectConnection:Disconnect()
        collectConnection = nil
    end
    
    disableNoclip()
    print("[MM2 Farm] ❌ АВТОФАРМ ВЫКЛЮЧЁН")
    print("[MM2 Farm] 📊 Собрано: " .. coinCount .. " монет")
end

-- ===== ПЕРЕКЛЮЧЕНИЕ =====
local function toggleFarming()
    if isFarming then
        stopFarming()
    else
        startFarming()
    end
end

-- ===== GUI С УПРАВЛЕНИЕМ ВЫСОТОЙ =====
local function createGUI()
    if player.PlayerGui:FindFirstChild("MM2FarmGUI") then
        player.PlayerGui.MM2FarmGUI:Destroy()
    end
    
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "MM2FarmGUI"
    screenGui.Parent = player.PlayerGui
    screenGui.ResetOnSpawn = false
    
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 260, 0, 200)
    frame.Position = UDim2.new(0, 10, 0, 10)
    frame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
    frame.BackgroundTransparency = 0.1
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Draggable = true
    frame.Parent = screenGui
    
    -- Заголовок
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 30)
    title.Position = UDim2.new(0, 0, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = " MM2 SMART FARM v3"
    title.TextColor3 = Color3.fromRGB(255, 215, 0)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = frame
    
    -- Статус
    local statusLabel = Instance.new("TextLabel")
    statusLabel.Size = UDim2.new(1, 0, 0, 22)
    statusLabel.Position = UDim2.new(0, 0, 0, 32)
    statusLabel.BackgroundTransparency = 1
    statusLabel.Text = "⚪ ОТКЛЮЧЁН"
    statusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
    statusLabel.TextScaled = true
    statusLabel.Font = Enum.Font.Gotham
    statusLabel.Name = "StatusLabel"
    statusLabel.Parent = frame
    
    -- Статистика
    local statsLabel = Instance.new("TextLabel")
    statsLabel.Size = UDim2.new(1, 0, 0, 18)
    statsLabel.Position = UDim2.new(0, 0, 0, 56)
    statsLabel.BackgroundTransparency = 1
    statsLabel.Text = " Собрано: 0"
    statsLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
    statsLabel.TextScaled = true
    statsLabel.Font = Enum.Font.Gotham
    statsLabel.Name = "StatsLabel"
    statsLabel.Parent = frame
    
    -- Высота
    local heightLabel = Instance.new("TextLabel")
    heightLabel.Size = UDim2.new(1, 0, 0, 18)
    heightLabel.Position = UDim2.new(0, 0, 0, 76)
    heightLabel.BackgroundTransparency = 1
    heightLabel.Text = "Высота: " .. currentHeight
    heightLabel.TextColor3 = Color3.fromRGB(100, 200, 255)
    heightLabel.TextScaled = true
    heightLabel.Font = Enum.Font.Gotham
    heightLabel.Name = "HeightLabel"
    heightLabel.Parent = frame
    
    -- Кнопки управления высотой
    local upBtn = Instance.new("TextButton")
    upBtn.Size = UDim2.new(0.3, 0, 0, 25)
    upBtn.Position = UDim2.new(0.05, 0, 0, 98)
    upBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 200)
    upBtn.Text = "▲ +1"
    upBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    upBtn.TextScaled = true
    upBtn.Font = Enum.Font.GothamBold
    upBtn.BorderSizePixel = 0
    upBtn.Name = "UpBtn"
    upBtn.Parent = frame
    
    local downBtn = Instance.new("TextButton")
    downBtn.Size = UDim2.new(0.3, 0, 0, 25)
    downBtn.Position = UDim2.new(0.65, 0, 0, 98)
    downBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 0)
    downBtn.Text = "▼ -1"
    downBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    downBtn.TextScaled = true
    downBtn.Font = Enum.Font.GothamBold
    downBtn.BorderSizePixel = 0
    downBtn.Name = "DownBtn"
    downBtn.Parent = frame
    
    -- Кнопка включения
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Size = UDim2.new(0.8, 0, 0, 32)
    toggleBtn.Position = UDim2.new(0.1, 0, 0, 135)
    toggleBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
    toggleBtn.Text = "🟢 ВКЛЮЧИТЬ (F1)"
    toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    toggleBtn.TextScaled = true
    toggleBtn.Font = Enum.Font.GothamBold
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Name = "ToggleButton"
    toggleBtn.Parent = frame
    
    -- Обновление UI
    local function updateUI()
        local status = screenGui.Frame.StatusLabel
        local btn = screenGui.Frame.ToggleButton
        local stats = screenGui.Frame.StatsLabel
        local height = screenGui.Frame.HeightLabel
        
        if isFarming then
            status.Text = "🟢 ВКЛЮЧЁН"
            status.TextColor3 = Color3.fromRGB(50, 255, 50)
            btn.Text = "🔴 ВЫКЛЮЧИТЬ (F1)"
            btn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
        else
            status.Text = "⚪ ОТКЛЮЧЁН"
            status.TextColor3 = Color3.fromRGB(255, 50, 50)
            btn.Text = "🟢 ВКЛЮЧИТЬ (F1)"
            btn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
        end
        
        stats.Text = " Собрано: " .. coinCount
        height.Text = "Высота: " .. currentHeight
    end
    
    -- Обработчики кнопок
    toggleBtn.MouseButton1Click:Connect(function()
        toggleFarming()
        updateUI()
    end)
    
    upBtn.MouseButton1Click:Connect(function()
        changeHeight(1)
        updateUI()
    end)
    
    downBtn.MouseButton1Click:Connect(function()
        changeHeight(-1)
        updateUI()
    end)
    
    -- Автообновление статистики
    spawn(function()
        while screenGui and screenGui.Parent do
            task.wait(1)
            local stats = screenGui:FindFirstChild("Frame") and screenGui.Frame:FindFirstChild("StatsLabel")
            if stats then
                stats.Text = " Собрано: " .. coinCount
            end
        end
    end)
    
    return { updateUI = updateUI }
end

-- ===== ГОРЯЧАЯ КЛАВИША =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == config.toggleKey then
        toggleFarming()
        local gui = player.PlayerGui:FindFirstChild("MM2FarmGUI")
        if gui then
            local status = gui.Frame:FindFirstChild("StatusLabel")
            local btn = gui.Frame:FindFirstChild("ToggleButton")
            if status and btn then
                if isFarming then
                    status.Text = "🟢 ВКЛЮЧЁН"
                    status.TextColor3 = Color3.fromRGB(50, 255, 50)
                    btn.Text = "🔴 ВЫКЛЮЧИТЬ (F1)"
                    btn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
                else
                    status.Text = "⚪ ОТКЛЮЧЁН"
                    status.TextColor3 = Color3.fromRGB(255, 50, 50)
                    btn.Text = "🟢 ВКЛЮЧИТЬ (F1)"
                    btn.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
                end
            end
        end
    end
end)

-- ===== ОБРАБОТКА РЕСПАУНА =====
player.CharacterAdded:Connect(function(newChar)
    character = newChar
    humanoid = newChar:FindFirstChild("Humanoid")
    rootPart = newChar:FindFirstChild("HumanoidRootPart")
    if isFarming then
        task.wait(0.5)
        enableNoclip()
    end
end)

-- ===== ЗАПУСК =====
createGUI()

print("========================================")
print("   MM2 SMART COIN FARM v3")
print("  🔹 Нажми F1 для включения/выключения")
print("  🔹 Кнопки ▲▼ для регулировки высоты")
print("  🔹 Подбери высоту, чтобы собирать монеты")
print("  🔹 Рекомендуется: от -3 до -12")
print("========================================")
